.sheet-backdrop { position: fixed; inset: 0; z-index: 20; background: rgba(0,0,0,.4); display: flex; align-items: flex-end; }
.choice-sheet { width: 100%; margin: 0; padding-bottom: calc(32rpx + env(safe-area-inset-bottom)); }
